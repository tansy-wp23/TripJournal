/// Auth is a teammate's module (see CLAUDE.md). This mocks a single
/// logged-in userId until the real session/auth provider lands.
const kMockUserId = '00000000-0000-0000-0000-000000000001';

/// Mock display name for the homepage top bar, until real auth lands.
const kMockUserDisplayName = 'Sang You';
